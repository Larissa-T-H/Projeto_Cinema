@model Cinema.Models.Sala

@{
    ViewData["Title"] = "Detalhes";
}

    <h1>Detalhes</h1>

    <div>
        <h4>Sala</h4>
        <hr />
        <dl class="row">
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.SalaNum)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.SalaNum)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model.SalaId"class="btn btn-outline-warning">Editar</a> 

        <a asp-action="Index"class="btn btn-outline-secondary">Voltar</a>
    </div>

